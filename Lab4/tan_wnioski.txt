Wyniki programu przedstawiają:
-Interwał początkowy
-Ilość iteracji
-Zastosowany algorytm
-Znalezioną wartość fzero
-Zbieżność

W pierwszym przypadku wyszukano zero dla funkcji tan(x) w okolicy 6. Znaleziono wartość około 6.2832, co jest bliskie wartości pi. Oznacza to, że funkcja tangens ma miejsce zerowe przy tej wartości.

W drugim przypadku poszukiwał miejsca zerowego funkcji tan(x) w okolicy 4.5. Jednakże w tych okolicach nie znajduje sie przecięcie wykresu a jego asymptota. Liczbą iteracji jest znacznie większą oraz pojawiają się wyraźne wzrosty wartości f(x). Funkcja często zmienia stosowany algorytm.